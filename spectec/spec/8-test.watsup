;; def $foo() : nat*
;; def $foo() = y*
;;   -- if x* = 0 1 2 3 4 5
;;   -- if start* = (START x)*
;;   -- if start* = (START y)*

;; def $foo() : (nat, nat*, nat?)
;; def $foo() = (n, y^n, y'?)
;;   -- if x^n = 0 1 2 3 4 5
;;   -- if start^n = (START x)^n
;;   -- if start^n = (START y)^n
;;   -- if x'? = 1
;;   -- if start'? = (START x')?
;;   -- if start'? = (START y')?

;; def $foo() : nat
;; def $foo() = 0
;;   -- if x^3 = 0 1 2
;;   -- if y^3 = 0 1 2
;;   -- if (x^3[i] = y)^(i<3)
